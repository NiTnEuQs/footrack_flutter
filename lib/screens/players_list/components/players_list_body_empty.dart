import 'package:flutter/material.dart';

class PlayersListBodyEmpty extends StatelessWidget {
  const PlayersListBodyEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Aucun joueur"),
    );
  }
}
