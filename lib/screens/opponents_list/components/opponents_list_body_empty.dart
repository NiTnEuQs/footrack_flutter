import 'package:flutter/material.dart';

class OpponentsListBodyEmpty extends StatelessWidget {
  const OpponentsListBodyEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Aucun adversaire"),
    );
  }
}
