name: footrack_front
description: Footrack is an application to help football managers managing their seasons

publish_to: 'none'
version: 1.2.1+4

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- UI

  cupertino_icons: ^1.0.5
  flutter_layout_grid: ^2.0.0

  # --- State management

  flutter_riverpod: ^2.5.1
  freezed_annotation: ^3.1.0

  # --- Firebase

  #  google_sign_in: ^5.3.2
  firebase_core: ^2.28.0
  firebase_remote_config: ^4.4.0
  firebase_analytics: ^10.10.0
  firebase_auth: ^4.19.0
  #  firebase_messaging: ^14.0.3
  cloud_firestore: ^4.16.0
  flamingo: ^3.0.0
  flamingo_annotation: ^1.0.0

  # --- Misc

  skeletonizer: ^2.1.0+1
  google_fonts: ^6.3.1
  collection: ^1.17.2
  intl: ^0.20.2
  wakelock_plus: ^1.2.8
  json_annotation: ^4.8.1
  shared_preferences: ^2.5.3
  package_info_plus: ^9.0.0
#  flutter_appcenter_bundle: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # --- API

  build_runner: ^2.4.6
  #  json_serializable: ^6.5.4
  #  retrofit_generator: ^4.0.1
  #  cloud_firestore_odm_generator: ^1.0.0-dev.38
  flamingo_generator: ^1.0.0

  # --- Misc

  #  freezed: ^2.0.3+1
  #  lint: ^1.8.2

flutter:
  uses-material-design: true




